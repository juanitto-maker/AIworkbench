preferences & instructions:

-user is using/working only on android mobil: no terminal, no console

-when images in context for refference, context them but not push them if they are tweaks shreenshots

-push images ONLY if they'll be part of app (like backgrounds, icons, etc)

-make all the notifications, popups, confirmation or upload or etc boxes, matching the app style/theme

-make the app standalone pwa

-the api keys only in deployment secret variables (netlify, vercel, etc)

-set and read the PUBLIC_APP_URL environment variable to set the HTTP Referer header for all server-side API calls to satisfy the domain restriction and remind the user in the task summary after completion to set the PUBLIC_APP_URL and URL permissions in API key settings.

-name the main file "index.html"
